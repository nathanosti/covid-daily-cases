export type covidCasesProps = {
  _id?: string;
  location: string;
  date: string;
  variant: string;
  num_sequences: string;
  perc_sequences: string;
  num_sequences_total: string;
  created_at?: Date;
};
